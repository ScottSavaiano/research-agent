---
name: generate-synthetic-data
description: Conducts the research agent's EXECUTION-regime synthetic-data stage — generating synthetic or simulated data AS the legitimate studied object when the SEALED methodology specifies it, validated by algorithmic fidelity against a real benchmark and labeled synthetic wherever written. It is the third leg of the three-way synthetic-data distinction (planning toy-surface in operationalize-construct → real-only in source-and-prepare-data → synthetic-as-object here) and NEVER a fallback for unreachable real data (that stays source-and-prepare-data's gated-data route). The spine is menu-agnostic (specify the generator → generate + label → validate by fidelity); what "generate" concretely produces depends on the sealed method — the menu-shape layer covers shape E as-object (silicon samples; generative agent-based simulation; fine-tuned-model probe) and the cross-shape synthetic-as-auxiliary roles (privacy-preserving synthesis; ML augmentation; Monte-Carlo/bootstrap for inference stability; simulation for power/robustness). Fires in EXECUTION regime after the proposal is sealed (Stage 14), either seal-dispatched by the mentor or ceded to from source-and-prepare-data on a shape-E design; continues within the research agent's execution lane (does not return to the mentor), with R11 review-agent spot-checks at the sanity-check stopping points. Produces the labeled synthetic dataset staged in data/ plus a generation-and-fidelity record the student draws on to write the paper's Methods/Data section (R10 scaffold handoff). Discharges R7's synthetic-participant-runs clause. Status — first draft 2026-07-01 (Tier 6, execution pipeline; build-as-you-go, no gold exemplar yet).
---

# Generate Synthetic Data

**Last edited:** 2026-07-01 (Cowork — **teacher-admin critique applied** (F4 'move'-noun→'step'; F5 fidelity-moment Family-2 scope note; F6 bright-line cede-in clause; F7 `unvalidated` verdict home named; F1 R8 reconcile-with-sibling marked resolved). Prior same day — **voice-read applied**: the fidelity-gate moment approved as-is; the 'bright line' moment **removed as a student-facing moment** (educator — the seal already settles whether synthesis is the design) and kept as an **internal routing guard**. Prior same day — **first draft**, authored against the three-way synthetic-data distinction (decision-log 2026-06-29), the `source-and-prepare-data` sibling structure, `operationalize-construct`'s shape-E validity framing (algorithmic fidelity), the research-agent SOUL's "I never invent data" reconciliation + careful-tier framing, R7 (synthetic-participant runs), and the R8(c) ruling (2026-07-01 — the research agent does **not** write `project_paper_status.md`; execution progress lives in execution-track records; stage advances mentor-side at the R10 return). No gold exemplar yet → **build-as-you-go**: spine specified, per-use forms are sketches.)
*Editing convention: see `00-handoff.md` → "Editing conventions". Status section at the bottom records the draft-vs-promoted lifecycle.*

> **Tier-6 reconciliation flags — status.** (1) **Dispatch/return — RESOLVED** (execution-regime: seal-dispatched *or* ceded-to from `source-and-prepare-data`; continues forward within execution; R11 spot-checks). (2) **Obligation — RESOLVED**: discharges **R7**'s synthetic-participant-runs clause; the real-data sibling `source-and-prepare-data` routes shape E here. (3) **Data-record home — RESOLVED** (own file `data/synthesis-provenance.md` + `decisions.md`; Methods prose via **R10**). (4) **R8 — RESOLVED to candidate (c)** (2026-07-01): this skill does **not** write `project_paper_status.md`; it writes execution-track records only, and the stage advances mentor-side when the student returns for the writing handoff. (5) **Build-as-you-go**: only the spine + the shape-E-as-object frame are specified; the per-use forms are sketches fleshed out as gold exemplars are built.

## What this skill is

The research agent's **execution-regime synthetic-data stage**: when the **sealed methodology** calls for synthetic or simulated data as the thing the study actually analyzes, this skill generates it, validates it against a real benchmark, and stages it — with a written record of *how it was generated* and *why it can be trusted.* It is the execution sibling of `source-and-prepare-data`, and the **synthetic-as-object** third leg of the three-way distinction:

- `operationalize-construct` (planning) — may build a **toy/synthetic exploration surface** for *trying* operationalizations only; **never analyzed, never validates** a measure, never enters the paper.
- `source-and-prepare-data` (execution) — acquires **real data only**; never generates analyzed data; if no real dataset is reachable it routes to the research teacher or faculty mentor (gated access), **never** substitutes synthetic.
- **`generate-synthetic-data` (execution, this skill)** — generates synthetic/simulated data **as the legitimate studied object**, only when the **sealed methodology** specifies it, validated by **algorithmic fidelity** against a real benchmark, labeled synthetic wherever written.

**The dividing line between the toy surface and this skill is the *method*.** Synthetic-as-object must be the **chosen, sealed design** — not a workaround reached for because real data is hard to get. If synthesis is being considered *because* the real dataset is unreachable, that is not this skill: it is `source-and-prepare-data`'s gated-data route (record the block, route to the research teacher/faculty mentor). This skill runs only on a design that *chose* synthesis on the merits and froze that choice at the seal.

By completion the student has:

- A **labeled synthetic dataset** staged in the workspace `data/` folder (clearly named synthetic), plus the **generator itself** — the script / prompt set / agent configuration, with the model version and seeds pinned so the run reproduces.
- A **generation & fidelity record** (`data/synthesis-provenance.md`) recording: the generation spec (model + version, personas/prompts/parameters, seeds, provider, date); the **fidelity benchmark and its result**; and the explicit "labeled synthetic" declaration — the trail the Methods/Data section and the review agent both draw on.
- A clear **fidelity verdict**: the synthetic data reproduced a known real benchmark for the checkable cases (trustworthy for the novel cases), or it did not (not analysis-ready — revisit the generator, or post-seal report the shortfall as a deviation).

It never presents synthetic data as empirical, never lets a synthetic set substitute for unreachable real data, and labels synthetic data **synthetic** wherever it is written — in `data/`, in the record, in the Methods, and in every figure and table.

## Reconciling the SOUL's "I never invent data"

The research-agent SOUL commits: *"I never invent data, and I never invent a source."* That rule is about never fabricating observations to stand in for reality or to manufacture findings. It is **not** contradicted here — the SOUL itself names "synthetic-participant runs" as a legitimate data mode. Generating synthetic data is legitimate, not fabrication, when **all three** hold:

1. **Sealed design, not convenience.** Synthesis is the methodology the student *chose and sealed*, on the merits — never a fallback for data they could not obtain.
2. **Fidelity-validated.** The synthetic data reproduces a known real benchmark for the cases that can be checked, before its novel outputs are trusted. Un-validated synthetic data certifies nothing (the direct analogue of `operationalize-construct`'s "a toy set never yields a `validated` verdict").
3. **Labeled synthetic everywhere.** Nothing generated is ever presented as an empirical observation.

Strip any one of the three and it collapses back into "inventing data" — which is exactly what the SOUL forbids. This skill's whole job is to hold all three.

## When this skill fires

In the research agent's **execution regime**, after the proposal is **sealed** (Stage 14 — the methodology, including the synthetic design, is frozen) and before the analysis runs (Stages 16–18, as-executed). Two entry paths:

- **Seal-dispatched** — the mentor composes the concrete handoff into execution (the R1/R10 pattern: the concrete switch to the research agent — icon-click on desktop; `research` on the TUI — plus a prompt naming the sealed synthetic methodology). The research agent recognizes the arriving handoff per R1/R9 and reads the named files first.
- **Ceded-to from `source-and-prepare-data`** — when that skill hits a **shape-E** design (or a declared cross-shape synthetic role), it cedes here rather than generating analyzed data itself.

**Why generation waits for the seal (deliberate, not an oversight).** Same logic as `source-and-prepare-data`: planning (Stage 6.5) validates the *operationalization* — for a synthetic design, whether the persona/probe/agent-rule can in principle carry the construct — on cheap, revisable ground. Standing up the full generator and its fidelity benchmark before the seal would be **wasted effort** if the design still changes (a failed planning check bounces back to revise the research question; the §7 stale map cascades). The seal is the commitment point that makes the one-time heavy generation-and-validation work worth doing. Division: **validation of the idea (planning, cheap, revisable)** versus **generation of the object (execution, post-seal, committed)**.

## Dispatch and the handoff (the execution-regime shape)

Like `source-and-prepare-data`, this skill lives in **execution**, the research agent's own regime — it does not return to the mentor on clean completion.

- **Inbound** — seal-dispatched (R1/R10) or ceded-to from `source-and-prepare-data` (shape E). The agent reads the sealed methodology + the Stage-6.5 operationalization record first.
- **Forward, not back.** On a clean completion this skill hands **forward to analysis** (`choose-analysis-method` / `run-analysis`, Tier 6), which preconditions on the validated synthetic dataset + the fidelity record. Execution stays the research agent's lane.
- **R11 review-agent spot-checks along the way (two named points).** After a consequential execution task the research agent initiates a concrete handoff sending the student to the **review agent** for AI-error spot-checks (R11; V5 function 2) — the *sanity-check stopping points*. This skill has two:
  - **after Phase 2 (generation)** — "did the generator run the sealed spec? is the model version pinned and recorded? are the outputs degenerate — duplicated rows, mode-collapse, a persona that ignored its conditioning? does the N match what was requested?";
  - **after Phase 3 (fidelity validation)** — "does the benchmark actually calibrate this data, or was it cherry-picked? is the fidelity claim honest about what it did and did not check? are the un-checkable novel outputs being trusted further than the benchmark warrants?"
  These are exactly the checks V5 function 2 names, on the surface most exposed to silent agent error; routing them to the dedicated guard (rather than self-checking) is the point.
- **R8 (position file) — candidate (c).** This skill does **not** write `project_paper_status.md`. Execution progress lives in the execution-track records it *does* own (`data/`, `data/synthesis-provenance.md`, `decisions.md`); the paper/position stage advances **mentor-side** when the student returns for the writing handoff (`project-walkthrough` receives the return and advances the stage; `paper-walkthrough` dispatches `scaffold-section`). The research agent signals via the student's return, never by writing the position file (2026-07-01 ruling; aligns with V2/M1).
- **The fidelity-failure loop-back.** If the synthetic data cannot reproduce the benchmark, the handoff loops **back** — to `operationalize-construct` (if a planning revision is still open) or, **post-seal**, the shortfall is refined against and **reported as a §6 deviation** in the as-executed Methods, never a silent rewrite of the frozen hypotheses.

## What "generate" means depends on the method — the menu-shape layer

The three-phase spine below is **menu-agnostic** — specify the generator, generate + label, validate by fidelity — but what "generate" concretely produces depends on the sealed method. This skill spans two use-families:

### Family 1 — Synthetic as the *object* (shape E)

The findings come *from* the synthetic data; validity is algorithmic fidelity against a real benchmark (mirrors `operationalize-construct` shape E).

- **E1 — Silicon samples / synthetic AI survey participants.** *Persona-conditioned LLM responses as respondent data (Argyle "silicon sampling"; Horton "homo silicus").* Generate: a persona/conditioning scheme + the elicited measure, one simulated respondent at a time. Fidelity: reproduce known real marginals/subgroup patterns from a real survey (ANES, GSS) for the checkable cells before trusting the novel ones. *(Sketch; per-exemplar.)*
- **E2 — Generative agent-based simulation.** *Interacting agents producing emergent social dynamics (Park generative agents; OASIS / MiroFish).* Generate: agent rules + a generative push + the emergent outcome measure. Fidelity: reproduce a known stylized fact / real dynamic the system should recover, before reading off the counterfactual runs. *(Sketch; per-exemplar.)*
- **E3 — Fine-tuned-model probe.** *Fine-tune a model on a community corpus, then probe it (Jiang CommunityLM).* Generate: the fine-tune + the probe protocol. Fidelity: validate the probe against a real benchmark for that community (CommunityLM probed against ANES). *(Straddles shape D on the surface — the probe looks D-like — but the validity step is E's: calibrate against a real benchmark. Sketch; per-exemplar.)*

### Family 2 — Synthetic as a *subordinate auxiliary* (cross-shape roles)

The **real data is still the object**; the synthetic data plays a declared, sealed supporting role. Labeled synthetic and fidelity-checked all the same, but it never becomes the finding.

- **Privacy-preserving synthesis.** Generate a synthetic twin of a sensitive real dataset so the analysis is shareable. Fidelity: the synthetic preserves the real distributions and key relationships (utility) without re-identifying anyone. *(Sketch.)*
- **ML augmentation.** Synthesize additional training rows for a classifier/measurement model. Fidelity: held-out performance gain **on real data**, no train/test leakage. *(Sketch.)*
- **Monte-Carlo / bootstrap for inference stability.** Resample or simulate to obtain CIs, permutation nulls, or a sampling distribution. This simulates *the estimator*, not fabricated observations. Fidelity: the resampling scheme is correct and reported as such. *(Sketch.)*
- **Simulation for power / robustness.** Simulate data under pre-specified assumed effects to compute power or stress-test the design. Fidelity: the generating model matches the pre-registered assumptions; results are reported as **simulation**, never as empirical findings. *(Sketch.)*

**Build-as-you-go flag (load-bearing, mirrors the two sibling skills).** There is **no gold exemplar in this skill yet**, so **every use-form above is a sketch.** Each is fleshed out to full detail when a gold exemplar in that use is built. Until a use has an exemplar, a project in it runs the spine using the sketch **plus a declared deferred-scope note** in `data/synthesis-provenance.md` — so the gap is explicit, never latent. (Precedent: `source-and-prepare-data` shape A specified / B–F sketched; `operationalize-construct` the same.)

## The phases (execution — specify, generate, validate)

### Phase 1 — Specify the generator (from the sealed method)
Pin down exactly what is being generated and how, drawn from the sealed methodology and the Stage-6.5 operationalization record — the persona/conditioning + elicited measure (E1), the agent rules + emergent outcome (E2), the fine-tune corpus + probe (E3), or the declared cross-shape role (Family 2). Record the **generation spec** in `data/synthesis-provenance.md`: model + exact version, prompts/personas/parameters, random seeds, provider, and date (an LLM's outputs drift with model version, so the version is part of provenance, not a footnote). Name the **real benchmark** the fidelity check (Phase 3) will use — chosen *now*, before generating, so it is not reverse-engineered to pass.

**The bright-line gate (internal — not a student question).** Before generating anything, confirm this is the **sealed synthetic design**, not a reach-around for unreachable real data. This is an **internal routing check keyed on the seal and the dispatch, not a conversation to have with the student** — by the time this skill fires the methodology is sealed, so synthesis is already the chosen design; the check simply guards the one pathological case where synthesis is being reached for *because* real data turned out unreachable. In that case STOP: that is `source-and-prepare-data`'s gated-data route (record the block; route to the research teacher or faculty mentor), never a silent substitution here. A **cede-in from `source-and-prepare-data`** is, by construction, a genuine **shape-E chosen-design** cede — never a gated-data reroute (that sibling's unreachable-real-data case exits to the research teacher, and never here) — so this guard's pathological case can only arise from a **mis-sealed design**, not from the sibling's handoff. *(An earlier draft surfaced this as a scripted student question; removed 2026-07-01 on educator ruling — the seal already settles whether synthesis is the design, so asking would re-open a closed question. The guard survives as this internal rule plus the "Synthesis reached for as a workaround" edge case.)*

### Phase 2 — Generate + label
Run the generator to produce the synthetic dataset/run, **labeled synthetic wherever it is written** (file names, the record, later the Methods and every figure). Keep the full **generation log** — model version, seeds, parameters, date — so the run reproduces; a synthetic dataset whose generator cannot be re-run is not a defensible object. Stage the labeled dataset (and the generator script/config) in `data/`. Major generation choices also go to `decisions.md` (five-field).

→ **R11 spot-check (generation).** Hand the student to the review agent: did the generator run the sealed spec, is the version pinned, are the outputs non-degenerate, does the N match.

### Phase 3 — Validate by algorithmic fidelity (the integrity gate)
Before the synthetic data is declared analysis-ready: **reproduce the pre-named real benchmark** for the cases you can check, to earn trust in the novel outputs you cannot. The check is use-specific — silicon-sample marginals vs ANES/GSS; a generative simulation recovering a known stylized fact; a fine-tuned probe vs a real survey; a privacy-twin's distributions/correlations vs its real source; augmentation's held-out gain on real data; MC/bootstrap coverage; power-sim calibration. Record the benchmark, the comparison, and the outcome.

A fidelity **failure** = the synthetic data is not trustworthy: **do not analyze it.** Either the generator needs revisiting (a fixable spec problem) or the design's synthetic premise does not hold up (a design-level problem). Telling those apart is the load-bearing call, so at the verdict the skill surfaces the research agent's **careful-tier** option (the SOUL's "How I run, and when to use a stronger model": suggest switching to the careful tier for this one decision, then dropping back). It does **not** reference the mentor's `model-escalation` meta-skill — that ships only in the project-mentor profile; the research agent's escalation surface is its own SOUL, which keeps the tiers model-agnostic. Fire it once, at the verdict — not per check.

> **Scripted moment — the fidelity gate** *(voice-read approved 2026-07-01).* Intent: no findings off un-calibrated synthetic data; slow down for the load-bearing version. *Agent:* "Here's the moment that decides whether any of this is usable. We agreed up front on a real benchmark this synthetic data should be able to reproduce — a number or a pattern we already know is true out in the world. If it hits that, we've earned some trust in the parts we *can't* check directly, and we can go on to analyze. If it misses, that's not a small thing: it means the generated data isn't behaving like reality, and nothing we'd find in it would be real. Two possibilities, and they're very different — one, the generator has a fixable bug and we re-run it; two, the whole synthetic premise doesn't hold for this question, which post-seal we don't quietly paper over — we report it as a deviation in your Methods. Telling those apart is exactly the kind of call worth switching to the careful tier for. Show me the benchmark: what did we expect it to reproduce, and what did it actually produce?"

*(Scope note — Family 1 vs Family 2: the moment's "nothing we'd find in it would be real" framing is written for the **synthetic-as-object** case (Family 1), where the finding comes from the synthetic data. For a **Family-2 auxiliary** use (MC/bootstrap, power-sim, augmentation, privacy-synthesis) the real data is still the object, so a fidelity miss means the synthetic **aid is discarded and the real-data finding stands** — not that "nothing is real"; the spoken line is adjusted to that when the use is auxiliary.)*

## What this skill writes to the workspace
- **`data/`** — the **labeled synthetic dataset** and the **generator** (script / prompt set / agent config, with seeds), staged locally; large generated artifacts are git-ignored like `articles/` (local-by-default), with the generator script kept so the data reproduces.
- **`data/synthesis-provenance.md`** — the generation spec (model + version, personas/prompts/params, seeds, provider, date), the fidelity benchmark + result, and the "labeled synthetic" declaration. This is the durable execution-track record — the synthetic-data analogue of `source-and-prepare-data`'s `data/data-provenance.md`.
- **`decisions.md`** — the five-field generation decisions (generator design, benchmark choice, major parameter/persona choices).
- **does NOT write `project_paper_status.md`** (R8 candidate c). Execution progress lives in the records above; the paper/position stage advances mentor-side at the R10 return.
- **feeds, does not write — the Methods/Data prose.** The **Methods/Data (synthetic-data-generation) section is the student's**, written via an **R10 `scaffold-section` handoff** that draws on this record (function-not-content: the skill writes the generation-and-fidelity log, the student writes the section). Per the 2026-07-01 R10 ruling, the handoff is a **return to the mentor** — the research profile does not carry `scaffold-section`.

## Cross-agent handoff
Seal-dispatched by the mentor (R1/R10 inbound) or ceded-to from `source-and-prepare-data` on a shape-E design. On clean completion, hands **forward** to analysis (within execution), with **R11** review-agent spot-checks at the two stopping points above and an **R10** writing handoff (return to the mentor) for the Methods/Data section. On a **fidelity failure**, the handoff loops **back** to `operationalize-construct` (planning) or is refined-and-reported post-seal (§6). If it turns out the real reason for synthesis was unreachable real data, it routes **back to `source-and-prepare-data`'s gated-data path** (research teacher / faculty mentor), never forward. Discharges **R7**'s synthetic-participant-runs clause.

## Edge cases
- **Synthesis reached for as a workaround.** Out of scope by rule — the Phase-1 bright-line gate: if real data is merely unreachable, route to `source-and-prepare-data`'s gated-data path (research teacher / faculty mentor), never synthesize. Synthetic-as-object must be the sealed, chosen design.
- **The synthetic data fails fidelity.** The Phase-3 gate: pre-seal → `operationalize-construct`; post-seal → refine and report the deviation (§6). Not a silent rewrite of the hypotheses; not "analyze it anyway."
- **Model-version drift.** LLM outputs change with the model version; the version + seeds are part of provenance, and a re-generation on a new version is a new run, re-validated — not a silent swap.
- **Cross-shape synthetic that quietly becomes the object.** In Family 2 the real data stays the finding; if an augmentation/privacy/simulation aid starts standing in for the real result, that is a design drift to surface, not proceed through.
- **Un-benchmarkable synthesis.** If no real benchmark exists to calibrate against, the fidelity verdict cannot be earned — the object is `unvalidated` — recorded as such in `data/synthesis-provenance.md` (the record this skill owns; it never writes `project_paper_status.md`), the shape-E analogue of `operationalize-construct`'s guard: surface it, do not treat novel outputs as trustworthy.
- **Student data / PII.** Same rule as the sibling: synthesizing *from* a sensitive real source (privacy-preserving synthesis) still handles that source under the program privacy rules and the consent-to-publicness IRB line; student-generated PII never enters shared analysis or any model.

## Status
**First draft 2026-07-01 (Tier 6, execution pipeline).** Split from `source-and-prepare-data` per decision-log 2026-06-29 (the three-way synthetic-data distinction); authored 2026-07-01. Reconciliations resolved at draft: dispatch/handoff (seal-dispatched or ceded-to; forward not back), R7 discharge, R11 spot-checks, data-record home, R8 candidate (c) (no position-file write), the menu-shape layer (Family 1 shape-E-as-object; Family 2 cross-shape auxiliary), careful-tier escalation via SOUL, the SOUL "never invent data" reconciliation. Remaining build items, not gates on the draft:
1. **Voice-read — DONE 2026-07-01.** The **fidelity-gate** moment approved as-is. The **bright-line** moment was **removed as a student-facing scripted moment** (educator ruling: the seal already settles whether synthesis is the design — no student question needed) and **retained as an internal routing guard** (Phase 1 + the workaround edge case). This skill now carries **one** scripted moment.
2. **Per-use forms** (E1/E2/E3 + the four Family-2 roles) — fleshed out as each gold exemplar is built (build-as-you-go); until then, run the sketch with a declared deferred-scope note.
3. **Downstream handshake** — `choose-analysis-method` / `run-analysis` should precondition on this skill's validated synthetic dataset + fidelity record (reciprocal of the `source-and-prepare-data` → analysis handshake).
4. **Teacher-admin critique pass** — the sibling skills each ran one; do the same before commit.
5. **Reconciled with `source-and-prepare-data`** (2026-07-01) — that skill's R8 references were corrected to candidate (c); both siblings now state R8(c) identically. (No longer open.)

Pairs with `operationalize-construct` (Stage 6.5, planning, toy surface) and `source-and-prepare-data` (execution, real data). Commits with the Tier-2/6 research-agent profile landing.
